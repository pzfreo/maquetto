{
  "version": 2,
  "buildCommand": "echo 'Before fetch:' && git rev-list --count HEAD && git remote -v && git fetch --unshallow origin 2>&1 || echo 'unshallow failed, trying deep fetch' && git fetch --depth=100000 origin main 2>&1 || true && echo 'After fetch:' && git rev-list --count HEAD && pnpm build",
  "outputDirectory": "packages/frontend/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/((?!api/)(?!assets/).*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/((?!api/).*)",
      "headers": [
        {
          "key": "Cross-Origin-Embedder-Policy",
          "value": "require-corp"
        },
        {
          "key": "Cross-Origin-Opener-Policy",
          "value": "same-origin"
        }
      ]
    }
  ]
}

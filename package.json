{
  "name": "maquette",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @maquette/frontend dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22"
  }
}
